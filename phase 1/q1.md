When cloning a new Ruby project.
 - we should run "bundle install" to download the dependencies and use rbenv to set up a new virtual environment for the project.
 - We have to check Gemfile to check the dependencies for the project bundle will install gems from Gemfile.