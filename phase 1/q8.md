~~~
def read_user_age(file_path) 
  file = File.open(file_path) 
  age = file.read.to_i 
  file.close 
  
  if age < 18 
    puts "User is a minor" 
  else 
    puts "User is an adult" 
  end
  age
end
~~~

## Errors in the above code
- no error handling
- Data integrity ["invalid_text".to_i] -> 0 (Integer)